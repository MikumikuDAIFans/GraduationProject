# 项目任务书

**更新日期**: 2026-04-18  
**当前基线**: 以当前工作区代码与测试结果为准  
**当前判断**: 项目已进入 **V7 当前基线已落地** 状态

## 当前目标

在 V7 主基线已经完成切换的前提下，继续做收口与清债，确保后续开发与汇报都基于统一、稳定、可验证的版本口径。

## 当前完成情况

### 已稳定存在于主链路/主界面的部分

- 事件、任务、提醒、助手会话等基础业务链路已存在
- Assistant 已支持 WebSocket 流式回复
- workflow 已接管 `send_message` / `send_message_stream`
- 助手当前会话、Inbox、Summary 等接口已存在
- 地图、天气、Google Calendar、提醒、WebSocket 主工作台链路仍在使用
- 前端 Store 拆分已完成大部分结构迁移，`workspace.ts` 已退化为兼容层
- 移动端/桌面端壳目录已建好

### 已被当前基线吸纳的 V6 内容

- 向量库与 Gemini Embedding
- 习惯学习/偏好学习服务
- 多轮对话槽位补全
- 扫描线冲突检测器
- 智能任务拆分服务
- Habit / TaskSplit 数据模型及迁移草稿

### 已落地的 V7 主基线内容

- LangGraph 工作流目录与运行链路
- ReAct 子图
- 条件化上下文收集节点
- 工具注册型写法
- 部分超时与外部上下文优化
- backend 全量测试通过
- workflow debug 接口已落地
- Alembic 迁移已实跑验证到 `head`

## 当前关键判断

### 当前版本归属

- 当前应归类为：**V7 已落地**
- 当前主要工作从“版本切换”转为“收口与清债”

### 当前主问题

当前不再是“新能力没接通”，而是：

- 数据迁移还没完全收口
- 可选依赖仍需正式运行说明
- `AssistantService` 仍偏大，后续可继续瘦身

## 关键决策

### 已确认

1. 当前项目已经完成 V7 主入口接管。
2. 文档口径需要同步从“V6 在建”切到“V7 已落地”。
3. 后续优先级应转向迁移收口、依赖说明与架构清债。

### 建议锁定

1. 近期优先目标应是**收口 V7**，而不是再并行开新版本分支。
2. 在迁移/依赖/体量问题收口前，不建议继续横向扩展更多 agent 分支。

## 当前风险

1. `AssistantService` 仍然过大，尚未彻底瘦身。
2. 向量能力仍依赖可选外部包，需补齐安装与运行说明。
3. 剩余弃用 warning 主要来自测试代码。

## 下一步任务

### P0

1. 收口数据库层
   - 继续审视历史数据库兼容与增量升级路径
2. 收口依赖层
   - 补齐向量依赖安装说明与运行方式
3. 收口编排层
   - 继续拆薄 `AssistantService`

### P1

1. 深化习惯学习链路
   - 明确最小可用 API 面与展示面
2. 优化复杂场景 ReAct 使用策略
   - 保持可控而不是泛滥路由
3. 清理弃用警告
   - 统一时间处理实现

### P2

1. 将更多 runtime helper 从 `AssistantService` 抽离
2. 视需要补充 workflow trace / summary 的展示层集成

## 当前建议的唯一下一步动作

先以 **V7 收口** 为目标做一次实现级清理：

1. 统一迁移与模型
2. 统一依赖与运行说明
3. 继续缩减主助手体量
